# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.EventsController.getAllEvents
GET     /:id                        controllers.EventsController.getEvent(id: Int)
DELETE  /:id                        controllers.EventsController.deleteEvent(id: Int)
PUT     /                           controllers.EventsController.updateEvent()
POST    /                           controllers.EventsController.createEvent()